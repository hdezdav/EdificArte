# Dependencies
node_modules/

# Build outputs
dist/
.astro/

# Wrangler / Cloudflare local simulation storage and assets
.wrangler/
worker-configuration.d.ts

# Environments & local secrets
.env
.env.production
.env.local
.dev.vars

# Migration evidence outputs may contain source metadata or backups.
migration-evidence/output/
migration-evidence/backups/
!migration-evidence/*.template.json
!migration-evidence/*.specimen.json

# Supabase CLI local state and secrets (never link this checkout remotely).
supabase/.branches/
supabase/.temp/
supabase/.env

# OS files
.DS_Store
Thumbs.db

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local agent system caches
.atl/
.claude/
.agents/
.codegraph/

# Hardhat compilation outputs (regenerated by `pnpm hardhat compile`)
artifacts/
cache/

# pnpm approve-builds config (local-only)
.pnpm-approve-builds.json

# Skills installer lock
skills-lock.json

# Tianquiztli / Mercado files/caches
tianquiztismercado
tianquiztlismercado
tianquiztlismercado/
tianquiztlismercado*

.vercel
OG/