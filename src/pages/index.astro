---
import Layout from '../layouts/Layout.astro';
import AiLoader from '../components/ui/AiLoader.astro';
import { MONUMENTS } from '../data/monuments';
import tianquiztlisProducts from '../data/tianquiztlis_products.json';
import { TOURS, TOUR_GUIDE } from '../data/tours';
import type { Tour } from '../data/tours';

type Category = 'todos' | 'turismo' | 'patrimonio' | 'cultura' | 'gastronomia' | 'museo' | 'tienda' | 'sitio-remoto';

interface Experience {
  id: string;
  title: string;
  description: string;
  image: string;
  category: Exclude<Category, 'todos'>;
  tagColor: 'emerald' | 'brand' | 'amber' | 'rose' | 'indigo' | 'purple';
  url: string;
  cta: string;
  rating?: number;
  reviewCount?: number;
  price?: string;
  images?: string[];
  sku?: string;
  videoUrl?: string;
  isVRAvailable?: boolean;
  meetingPoint?: string;
  currency?: string;
  duration?: string;
  highlights?: string[];
  isTour?: boolean;
  translations?: {
    es: { name: string; desc: string; category: string; cta: string; guide: string };
    en: { name: string; desc: string; category: string; cta: string; guide: string };
    señas: { name: string; desc: string; category: string; cta: string; guide: string; videoUrl: string };
  };
}

const experiencias: Experience[] = [
  {
    id: 'turismo-regenerativo',
    title: 'Turismo Regenerativo México',
    description:
      'Experiencias inmersivas con comunidades locales que impulsan el cambio social y la regeneración cultural.',
    image:
      'https://www.turismoregenerativomexico.com/wp-content/uploads/2025/08/Copia-de-DSCN4585-768x576.jpg',
    category: 'turismo',
    tagColor: 'emerald',
    url: 'https://turismoregenerativomexico.com',
    cta: 'Conoce más',
    rating: 4.95,
    reviewCount: 124,
    price: '$250 MXN',
  },
  {
    id: 'fundacion-edificate',
    title: 'Fundación Edificate',
    description:
      'Preservamos el patrimonio arquitectónico y cultural de México a través de la educación y la tecnología.',
    image:
      'https://www.fundacionedificate.org/wp-content/uploads/2025/01/Edificate.jpg',
    category: 'patrimonio',
    tagColor: 'brand',
    url: 'https://www.fundacionedificate.org/',
    cta: 'Visitar sitio',
    rating: 4.9,
    reviewCount: 98,
    price: 'Gratis',
  },
  {
    id: 'casa-lamm',
    title: 'Casa Lamm - Cultura & Academia',
    description:
      'Centro cultural en una casona porfiriana de la CDMX: exposiciones, librería, taller de arte y academia de historia del arte.',
    image:
      'https://stanzahotel.com/wp-content/uploads/2024/07/casa_lamm_1.jpg',
    category: 'cultura',
    tagColor: 'amber',
    url: 'https://casalamm.com.mx/',
    cta: 'Ver agenda',
    rating: 4.88,
    reviewCount: 156,
    price: 'Varios',
  },
  {
    id: 'mercado-san-juan',
    title: 'Mercado de San Juan',
    description:
      'Gastronomía local en su máxima expresión: quesos artesanales, carnes exóticas, dulces tradicionales y chef en residencia.',
    image:
      'https://images.unsplash.com/photo-1565299624946-b28f40a0ae38?auto=format&fit=crop&w=800&h=500&q=80',
    category: 'gastronomia',
    tagColor: 'rose',
    url: 'https://www.mercadodesanjuan.com.mx/',
    cta: 'Visitar mercado',
    rating: 4.92,
    reviewCount: 342,
    price: 'Gratis',
  },
  {
    id: 'cdmx-walks',
    title: 'CDMX Heritage Walks',
    description:
      'Recorridos guiados a pie por el Centro Histórico: arquitectura virreinal, murales contemporáneos y leyendas populares.',
    image:
      'https://images.unsplash.com/photo-1518105779142-d975f22f1b0a?auto=format&fit=crop&w=800&h=500&q=80',
    category: 'turismo',
    tagColor: 'emerald',
    url: 'https://cdmxheritage.com/',
    cta: 'Reservar tour',
    rating: 4.97,
    reviewCount: 88,
    price: '$180 MXN',
  },
  {
    id: 'museo-anahuacalli',
    title: 'Museo Anahuacalli',
    description:
      'Pirámide de basalto diseñada por Diego Rivera para albergar su colección de arte prehispánico, rodeada de reserva ecológica.',
    image:
      'https://cdn0.bodas.com.mx/vendor/0011/3_2/960/jpeg/vista-aerea-de-costado-dron_5_320011-162250734231083.jpeg',
    category: 'patrimonio',
    tagColor: 'brand',
    url: 'https://www.museoanahuacalli.org.mx/',
    cta: 'Conoce más',
    rating: 4.96,
    reviewCount: 210,
    price: '$80 MXN',
  },
];

// Generar experiencias a partir de los museos de Monuments
const museosExperiencias: Experience[] = MONUMENTS.filter((m) => m.type === 'museo').map((m) => {
  const ratings: Record<string, { rating: number; reviewCount: number; price: string }> = {
    'bellas-artes': { rating: 4.97, reviewCount: 382, price: '$85 MXN' },
    'palacio-nacional': { rating: 4.89, reviewCount: 145, price: 'Gratis' },
    'revolucion': { rating: 4.92, reviewCount: 204, price: '$120 MXN' },
    'angel': { rating: 4.96, reviewCount: 412, price: 'Gratis' },
    'chapultepec': { rating: 4.98, reviewCount: 520, price: '$90 MXN' },
  };

  const translations = {
    es: {
      name: m.name,
      desc: m.desc,
      category: m.category,
      cta: 'Ver guía',
      guide: `¡Bienvenido al ${m.name}! Este emblemático espacio es parte fundamental del patrimonio histórico de la Ciudad de México. Aquí podrás contemplar una arquitectura imponente y aprender sobre su evolución artística, social e histórica a través del tiempo. Disfrutá del recorrido detallado por sus salas principales y descubrí los secretos que sus paredes guardan.`,
    },
    en: {
      name: m.name === 'Palacio de Bellas Artes' ? 'Palace of Fine Arts' : (m.name === 'Palacio Nacional' ? 'National Palace' : (m.name === 'Castillo de Chapultepec' ? 'Chapultepec Castle' : (m.name === 'Ángel de la Independencia' ? 'Angel of Independence' : 'Monument to the Revolution'))),
      desc: m.name === 'Palacio de Bellas Artes' ? 'One of the world\'s most famous opera houses, known for its Art Nouveau exterior and Art Deco interior.' : m.desc,
      category: m.category === 'Museo / Teatro' ? 'Museum / Theater' : (m.category === 'Museo / Castillo' ? 'Museum / Castle' : 'Historical Monument'),
      cta: 'View guide',
      guide: `Welcome to the ${m.name}! This emblematic site is a key piece of Mexico City's historical heritage. Here, you can contemplate stunning architecture and learn about its artistic, social, and historical evolution through time. Enjoy the detailed self-guided tour through its main halls and uncover the secrets written in its walls.`,
    },
    señas: {
      name: `${m.name} (LSM)`,
      desc: 'Guía adaptada en Lengua de Señas Mexicana (LSM) con subtítulos y videos de alta visibilidad.',
      category: 'Accesibilidad / LSM',
      cta: 'Ver LSM',
      guide: `Esta es la guía accesible en Lengua de Señas Mexicana (LSM) para el ${m.name}. Explicamos la historia, el contexto y los murales o detalles artísticos más representativos para garantizar el acceso pleno a la cultura para la comunidad con discapacidad auditiva.`,
      videoUrl: 'https://www.youtube.com/embed/dQw4w9WgXcQ', // Video educativo genérico como demo
    },
  };

  const rData = ratings[m.id] || { rating: 4.8, reviewCount: 15, price: 'Gratis' };

  return {
    id: m.id,
    title: m.name,
    description: m.desc,
    image: m.image,
    category: 'museo',
    tagColor: 'indigo',
    url: '#',
    cta: 'Ver guía',
    rating: rData.rating,
    reviewCount: rData.reviewCount,
    price: rData.price,
    translations,
  };
});

// Generar experiencias a partir de los productos de Tianquiztlis Mercado (filtrando duplicados por nombre)
const uniqueTianquiztlisProducts = tianquiztlisProducts.filter(
  (p, index, self) => self.findIndex((t) => t.name === p.name) === index
);

const tiendaExperiencias: Experience[] = uniqueTianquiztlisProducts.map((p, index) => {
  return {
    id: `prod-${p.sku || index}`,
    title: p.name,
    description: p.description,
    image: p.images[0] || 'https://images.unsplash.com/photo-1513519245088-0e12902e5a38?auto=format&fit=crop&w=800&h=500&q=80',
    category: 'tienda',
    tagColor: 'purple',
    url: '#',
    cta: 'Ver Detalles',
    rating: parseFloat((4.75 + (index % 5) * 0.05).toFixed(2)),
    reviewCount: 14 + (index * 7) % 43,
    price: p.price ? `$${p.price} ${p.currency}` : 'Consultar',
    images: p.images,
    sku: p.sku
  };
});

// Generar experiencias para monumentos remotos (sitio-remoto), por ejemplo Pirámides del Sol.
// Estos monumentos NO aparecen en el mapa (filtrados en mapa-app.ts) pero sí en Explorar.
const sitioRemotoExperiencias: Experience[] = MONUMENTS.filter((m) => m.type === 'sitio-remoto').map((m) => {
  const translations = {
    es: {
      name: m.name,
      desc: m.desc,
      category: m.category,
      cta: 'Ver experiencia',
      guide: `¡Bienvenido a ${m.name}! Esta experiencia remota forma parte del patrimonio cultural de México. A través de esta guía podés conocer la historia, el contexto y los detalles más importantes del sitio, complementado con una experiencia inmersiva en realidad virtual (VR) que te permite recorrerlo sin salir de tu casa.`,
    },
    en: {
      name: m.name,
      desc: m.desc,
      category: m.category,
      cta: 'View experience',
      guide: `Welcome to ${m.name}! This remote experience is part of Mexico's cultural heritage. Through this guide you can learn the history, context, and most important details of the site, complemented by an immersive virtual reality (VR) experience that lets you tour it from anywhere.`,
    },
    señas: {
      name: `${m.name} (LSM)`,
      desc: 'Guía accesible en Lengua de Señas Mexicana (LSM) con subtítulos y descripción visual del sitio.',
      category: 'Accesibilidad / LSM',
      cta: 'Ver LSM',
      guide: `Esta es la guía accesible en Lengua de Señas Mexicana (LSM) para ${m.name}. Explicamos la historia, el contexto arqueológico y los detalles más representativos del sitio para garantizar el acceso pleno a la cultura para la comunidad con discapacidad auditiva.`,
      videoUrl: m.videoUrl || 'https://www.youtube.com/embed/dQw4w9WgXcQ',
    },
  };

  return {
    id: m.id,
    title: m.name,
    description: m.desc,
    image: m.image,
    category: 'sitio-remoto',
    tagColor: 'purple',
    url: '#',
    cta: 'Ver experiencia VR',
    rating: 4.95,
    reviewCount: 287,
    price: 'Gratis',
    translations,
    videoUrl: m.videoUrl,
    isVRAvailable: m.isVRAvailable,
  };
});

const todasLasExperiencias: Experience[] = [...experiencias, ...museosExperiencias, ...sitioRemotoExperiencias, ...tiendaExperiencias];

const tagStyles: Record<
  Experience['tagColor'],
  { badge: string; pill: string }
> = {
  emerald: {
    badge:
      'border-emerald-200/50 bg-emerald-50 text-emerald-700 dark:border-emerald-800/50 dark:bg-emerald-950/40 dark:text-emerald-400',
    pill: 'border-emerald-200 bg-emerald-50 text-emerald-700 dark:border-emerald-800 dark:bg-emerald-950/30 dark:text-emerald-400',
  },
  brand: {
    badge:
      'border-brand-200/50 bg-brand-50 text-brand-700 dark:border-brand-800/50 dark:bg-brand-950/40 dark:text-brand-400',
    pill: 'border-brand-200 bg-brand-50 text-brand-700 dark:border-brand-800 dark:bg-brand-950/30 dark:text-brand-400',
  },
  amber: {
    badge:
      'border-amber-200/50 bg-amber-50 text-amber-700 dark:border-amber-800/50 dark:bg-amber-950/40 dark:text-amber-400',
    pill: 'border-amber-200 bg-amber-50 text-amber-700 dark:border-amber-800 dark:bg-amber-950/30 dark:text-amber-400',
  },
  rose: {
    badge:
      'border-rose-200/50 bg-rose-50 text-rose-700 dark:border-rose-800/50 dark:bg-rose-950/40 dark:text-rose-400',
    pill: 'border-rose-200 bg-rose-50 text-rose-700 dark:border-rose-800 dark:bg-rose-950/30 dark:text-rose-400',
  },
  indigo: {
    badge:
      'border-indigo-200/50 bg-indigo-50 text-indigo-700 dark:border-indigo-800/50 dark:bg-indigo-950/40 dark:text-indigo-400',
    pill: 'border-indigo-200 bg-indigo-50 text-indigo-700 dark:border-indigo-800 dark:bg-indigo-950/30 dark:text-indigo-400',
  },
  purple: {
    badge:
      'border-purple-200/50 bg-purple-50 text-purple-700 dark:border-purple-800/50 dark:bg-purple-950/40 dark:text-purple-400',
    pill: 'border-purple-200 bg-purple-50 text-purple-700 dark:border-purple-800 dark:bg-purple-950/30 dark:text-purple-400',
  },
};

const categoryLabel: Record<Category, string> = {
  todos: 'Todos',
  turismo: 'Turismo',
  patrimonio: 'Patrimonio',
  cultura: 'Cultura',
  gastronomia: 'Gastronomía',
  museo: 'Museos',
  tienda: 'Tienda Artesanal',
  'sitio-remoto': 'Sitios Turísticos',
};

const categories: Exclude<Category, 'todos'>[] = ['turismo', 'patrimonio', 'cultura', 'gastronomia', 'museo', 'tienda', 'sitio-remoto'];

// Helper: convierte un Tour en Experience para mezclarlo en el carousel de Turismo
function toExperience(tour: Tour): Experience {
  return {
    id: tour.id,
    title: tour.title,
    description: tour.description,
    image: tour.image,
    category: 'turismo',
    tagColor: 'amber',
    url: '#',
    cta: 'Reservar',
    rating: 4.98,
    reviewCount: 67,
    price: `$${tour.pricePerPerson} MXN`,
    highlights: tour.highlights,
    meetingPoint: tour.meetingPoint,
    currency: tour.currency,
    duration: tour.duration,
    isTour: true,
  };
}
---

<Layout title="EdificARTE — Explorar">
  <script is:inline>
    // Distinguir entre la primera carga del sitio (inicio de sesión) y navegación interna
    if (!sessionStorage.getItem('edificarte_session_active')) {
      sessionStorage.setItem('edificarte_session_active', 'true');
      if (localStorage.getItem('edificarte_welcome_shown') === 'true') {
        window.location.href = '/mapa';
      }
    }
  </script>

  <script>
    const lang = localStorage.getItem('edificarte_lang');
    const banner = document.getElementById('lang-soft-cta');
    if (!lang && banner) {
      banner.classList.remove('hidden');
      document
        .getElementById('lang-soft-cta-link')
        ?.addEventListener('click', () => {
          window.location.href = '/mapa';
        });
    }
  </script>

  <!-- Banner suave si no hay idioma elegido -->
  <div
    id="lang-soft-cta"
    class="mb-4 flex hidden items-center justify-between gap-3 rounded-2xl border border-brand-200/50 bg-brand-50 px-4 py-3 text-sm text-brand-800 dark:border-brand-800/50 dark:bg-brand-950/40 dark:text-brand-200"
  >
    <span class="flex items-center gap-2">
      <span class="material-symbols-outlined text-[18px]">translate</span>
      Elegí tu idioma para empezar
    </span>
    <button
      id="lang-soft-cta-link"
      class="rounded-full bg-brand-500 px-3 py-1 text-xs font-semibold text-white hover:bg-brand-600"
      type="button"
    >
      Ir al mapa
    </button>
  </div>

  <div class="space-y-6">
    <!-- Header -->
    <div
      class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-brand-600 to-brand-800 p-6 text-white shadow-xl"
    >
      <div
        class="pointer-events-none absolute -bottom-10 -right-10 h-40 w-40 rounded-full bg-white/5 blur-xl"
      >
      </div>
      <h1 class="text-3xl font-extrabold tracking-tight">Explorar</h1>

      <!-- Search Input Container -->
      <div class="mt-4 relative w-full max-w-md">
        <div class="flex items-center rounded-full border border-white/20 bg-white/10 px-4 py-2.5 backdrop-blur-md transition-all focus-within:bg-white/15 focus-within:border-white/30">
          <span class="material-symbols-outlined mr-2 text-[20px] text-white/70">search</span>
          <input
            type="text"
            id="explore-search-input"
            placeholder="Buscar experiencias, patrimonio, cultura..."
            aria-label="Buscar experiencias, patrimonio, cultura"
            class="w-full min-w-0 border-none bg-transparent p-0 text-[16px] sm:text-[16px] font-medium text-white placeholder-white/60 outline-none focus:ring-0"
          />
        </div>
      </div>
    </div>

    <!-- Category Chips -->
    <div
      class="no-scrollbar -mx-4 flex gap-2 overflow-x-auto px-4 pb-1"
      role="tablist"
      aria-label="Filtrar por categoría"
    >
      {
        (Object.keys(categoryLabel) as Category[]).map((cat) => (
          <button
            type="button"
            data-filter={cat}
            class={`filter-chip rounded-full border px-4 py-2 text-[12px] font-semibold transition-all active:scale-95 ${
              cat === 'todos'
                ? 'border-brand-500 bg-brand-500 text-white shadow-sm'
                : 'border-slate-200/70 bg-white text-slate-700 hover:border-brand-200 hover:bg-brand-50/60 hover:text-brand-700 dark:border-slate-700 dark:bg-slate-900 dark:text-slate-300 dark:hover:border-brand-800 dark:hover:bg-brand-950/30 dark:hover:text-brand-400'
            }`}
            aria-pressed={cat === 'todos' ? 'true' : 'false'}
          >
            {categoryLabel[cat]}
          </button>
        ))
      }
    </div>

    <!-- Experience Sections (Horizontal Carousels) -->
    <div id="experiences-sections-container" class="space-y-6">
      {
        categories.map((cat) => {
          const catExp = cat === 'turismo'
            ? [...todasLasExperiencias.filter(exp => exp.category === cat), ...TOURS.map(toExperience)]
            : todasLasExperiencias.filter(exp => exp.category === cat);
          if (catExp.length === 0) return null;
          
          return (
            <section
              data-category={cat}
              class="category-section w-full py-2 border-b border-slate-100/60 dark:border-slate-800/60 last:border-0"
            >
              <div class="mb-3 flex items-center justify-between">
                <h2 class="font-bold text-base md:text-lg tracking-tight text-slate-800 dark:text-slate-100 flex items-center gap-1.5">
                  <span class="h-2 w-2 rounded-full bg-brand-500"></span>
                  {categoryLabel[cat]}
                </h2>
                <div class="flex items-center gap-1.5">
                  <button
                    type="button"
                    class="scroll-left-btn h-7 w-7 rounded-full border border-slate-200 dark:border-slate-800/50 hover:bg-slate-50 dark:hover:bg-slate-800 text-slate-500 dark:text-slate-400 flex items-center justify-center transition-all active:scale-90"
                    title="Deslizar izquierda"
                  >
                    <span class="material-symbols-outlined text-[16px]">chevron_left</span>
                  </button>
                  <button
                    type="button"
                    class="scroll-right-btn h-7 w-7 rounded-full border border-slate-200 dark:border-slate-800/50 hover:bg-slate-50 dark:hover:bg-slate-800 text-slate-500 dark:text-slate-400 flex items-center justify-center transition-all active:scale-90"
                    title="Deslizar derecha"
                  >
                    <span class="material-symbols-outlined text-[16px]">chevron_right</span>
                  </button>
                </div>
              </div>

              {cat === 'tienda' ? (
                <div class="scroll-container no-scrollbar -mx-1 flex snap-x snap-mandatory gap-4 overflow-x-auto px-1 pb-3 scroll-smooth">
                  {
                    (() => {
                      const chunks = [];
                      for (let i = 0; i < catExp.length; i += 4) {
                        chunks.push(catExp.slice(i, i + 4));
                      }
                      return chunks.map((chunk) => (
                        <div class="grid grid-cols-2 gap-3 w-[calc(100vw-32px)] sm:w-[704px] flex-none snap-start">
                          {chunk.map((exp) => (
                            <article
                              data-category={exp.category}
                              data-is-product="true"
                              data-title={exp.title}
                              data-desc={exp.description}
                              data-price={exp.price}
                              data-sku={exp.sku}
                              data-image={exp.image}
                              data-images={exp.images ? JSON.stringify(exp.images) : undefined}
                              class="exp-card group relative aspect-square overflow-hidden rounded-2xl border border-slate-200/50 dark:border-slate-800/50 bg-white dark:bg-slate-900 shadow-sm transition-all duration-300 hover:shadow-md cursor-pointer active:scale-[0.98]"
                            >
                              {/* Product Image */}
                              <img
                                src={exp.image}
                                alt={exp.title}
                                loading="lazy"
                                class="absolute inset-0 h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
                              />

                              {/* Glassmorphic Overlay for Title & Price */}
                              <div class="absolute inset-x-0 bottom-0 bg-gradient-to-t from-slate-950/90 via-slate-950/40 to-transparent p-3 pt-8 flex flex-col justify-end text-white">
                                <h3 class="text-[11px] font-bold truncate tracking-tight">{exp.title}</h3>
                                <p class="text-[9px] font-extrabold text-purple-300 mt-0.5">{exp.price}</p>
                              </div>
                            </article>
                          ))}
                        </div>
                      ));
                    })()
                  }
                </div>
              ) : (
                <div
                  class="scroll-container no-scrollbar -mx-1 flex snap-x snap-mandatory gap-4 overflow-x-auto px-1 pb-3 scroll-smooth"
                >
                  {catExp.map((exp) => (
                    <article
                      data-category={exp.category}
                      data-translations={exp.translations ? JSON.stringify(exp.translations) : undefined}
                      data-rating={exp.rating}
                      data-image={exp.image}
                      data-images={exp.images ? JSON.stringify(exp.images) : undefined}
                      data-price={exp.price}
                      data-sku={exp.sku}
                      data-is-product="false"
                      data-title={exp.title}
                      data-desc={exp.description}
                      data-id={exp.id}
                      data-is-vr={exp.isVRAvailable ? 'true' : 'false'}
                      data-vr-url={exp.videoUrl || ''}
                      data-tour-id={exp.isTour ? exp.id : undefined}
                      data-tour-title={exp.isTour ? exp.title : undefined}
                      data-tour-meeting={exp.isTour ? (exp.meetingPoint || '') : undefined}
                      data-tour-image={exp.isTour ? exp.image : undefined}
                      class={`exp-card group relative flex h-[350px] w-[250px] sm:w-[270px] flex-none snap-start flex-col overflow-hidden rounded-2xl border border-slate-200/50 dark:border-slate-800/50 bg-white dark:bg-slate-900 shadow-sm transition-all duration-300 hover:shadow-md ${exp.translations ? 'cursor-pointer' : ''}`}
                    >
                      {/* Image */}
                      <div class="relative h-44 w-full overflow-hidden rounded-t-2xl bg-slate-100 dark:bg-slate-850">
                        <img
                          src={exp.image}
                          alt={exp.title}
                          loading="lazy"
                          class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-105"
                        />
                        {/* VR Badge: aparece arriba a la derecha cuando hay experiencia VR */}
                        {exp.isVRAvailable && exp.videoUrl && (
                          <a
                            href={exp.videoUrl}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="absolute top-3 right-3 z-10 flex h-7 w-7 items-center justify-center rounded-full bg-purple-600 text-white shadow-md backdrop-blur-md transition-all hover:bg-purple-700 active:scale-95"
                            title="Ver en VR"
                            aria-label="Ver experiencia en Realidad Virtual"
                            onclick="event.stopPropagation()"
                          >
                            <span class="material-symbols-outlined text-[16px]">view_in_ar</span>
                          </a>
                        )}
                        <button
                          type="button"
                          class={`favorite-btn absolute ${exp.isVRAvailable ? 'top-12' : 'top-3'} right-3 z-10 rounded-full bg-white/80 dark:bg-slate-900/80 text-neutral-700 dark:text-neutral-300 backdrop-blur-sm hover:bg-white/90 dark:hover:bg-slate-900 hover:text-rose-500 dark:hover:text-rose-400 p-1.5 transition-all shadow-sm active:scale-90`}
                          title="Añadir a favoritos"
                        >
                          <svg class="h-4 w-4 fill-none stroke-current stroke-[2px]" viewBox="0 0 24 24">
                            <path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z" />
                          </svg>
                        </button>
                        <span
                          class={`absolute top-3 left-3 rounded-lg px-2 py-0.5 font-bold text-[10px] uppercase tracking-wider backdrop-blur-md border ${tagStyles[exp.tagColor].badge}`}
                        >
                          {categoryLabel[exp.category]}
                        </span>
                      </div>

                      {/* Body */}
                      <div class="flex flex-1 flex-col justify-between p-4 bg-white dark:bg-slate-900">
                        <div>
                          <p class="text-slate-400 dark:text-slate-500 text-[10px] font-bold uppercase tracking-wider">
                            México / Ciudad de México
                          </p>
                          <h2 class="text-sm font-bold tracking-tight text-slate-800 dark:text-slate-100 mt-1 line-clamp-1 group-hover:text-brand-600 dark:group-hover:text-brand-400 transition-colors">
                            {exp.title}
                          </h2>
                          <p class="text-slate-500 dark:text-slate-400 text-xs tracking-tight mt-1.5 line-clamp-2 leading-relaxed">
                            {exp.description}
                          </p>
                        </div>

                        {/* Highlights (solo para tours) */}
                        {exp.isTour && exp.highlights && (
                          <div class="px-3 pt-2 pb-1 flex flex-wrap gap-1">
                            {exp.highlights.slice(0, 2).map((h) => (
                              <span class="text-[9px] bg-amber-50 text-amber-700 px-1.5 py-0.5 rounded-full border border-amber-200/50 font-semibold">
                                {h.length > 30 ? h.substring(0, 30) + '…' : h}
                              </span>
                            ))}
                          </div>
                        )}

                        {/* Footer */}
                        <div class="mt-auto pt-3 flex items-center gap-1.5 text-[11px] font-semibold text-slate-700 dark:text-slate-300 border-t border-slate-100 dark:border-slate-800/60">
                          {exp.rating && (
                            <span class="flex items-center gap-0.5 text-amber-500">
                              <span class="material-symbols-outlined text-[14px] !font-bold">star</span>
                              <span class="text-slate-800 dark:text-slate-200">{exp.rating.toFixed(2)}</span>
                            </span>
                          )}
                          {exp.reviewCount && (
                            <span class="text-slate-400 dark:text-slate-500 font-normal">
                              ({exp.reviewCount})
                            </span>
                          )}
                          {exp.price && (
                            <span class="text-slate-400 dark:text-slate-500 font-normal ml-0.5">
                              · {exp.price}
                            </span>
                          )}

                          {
                            exp.isTour ? (
                              <button
                                type="button"
                                class="ml-auto inline-flex items-center gap-1 rounded-xl bg-gradient-to-r from-amber-500 to-orange-600 hover:from-amber-600 hover:to-orange-700 text-white font-bold px-3 py-1.5 text-[11px] shadow-sm transition-all active:scale-[0.97] tour-reserve-btn"
                              >
                                <span class="material-symbols-outlined text-[12px]">event_available</span>
                                {exp.cta}
                              </button>
                            ) : exp.translations ? (
                              <button
                                type="button"
                                class="ml-auto inline-flex items-center gap-1 rounded-xl bg-brand-50 hover:bg-brand-100 dark:bg-brand-950/20 dark:hover:bg-brand-950/40 text-brand-600 dark:text-brand-400 font-bold px-3 py-1.5 text-[11px] shadow-sm transition-all active:scale-[0.97] guide-trigger-btn"
                              >
                                {exp.cta}
                                <span class="material-symbols-outlined text-[12px] transition-transform duration-200 group-hover:translate-x-0.5">arrow_forward</span>
                              </button>
                            ) : (
                              <a
                                href={exp.url}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="ml-auto inline-flex items-center gap-1 rounded-xl bg-brand-50 hover:bg-brand-100 dark:bg-brand-950/20 dark:hover:bg-brand-950/40 text-brand-600 dark:text-brand-400 font-bold px-3 py-1.5 text-[11px] shadow-sm transition-all active:scale-[0.97]"
                              >
                                {exp.cta}
                                <span class="material-symbols-outlined text-[12px] transition-transform duration-200 group-hover:translate-x-0.5">arrow_forward</span>
                              </a>
                            )
                          }
                        </div>
                      </div>
                    </article>
                  ))}
                </div>
              )}
            </section>
          );
        })
      }

      <!-- Empty state (shown by JS when no matches) -->
      <div
        id="empty-state"
        class="hidden flex-col items-center gap-2 rounded-2xl border border-dashed border-slate-300/60 bg-white/60 p-10 text-center dark:border-slate-700/60 dark:bg-slate-900/40"
      >
        <span class="material-symbols-outlined text-3xl text-slate-400"
          >search_off</span
        >
        <p class="text-sm font-semibold text-slate-700 dark:text-slate-300">
          No hay experiencias en esta categoría todavía
        </p>
        <p class="text-[12px] text-slate-500 dark:text-slate-400">
          Probá con otra categoría o volvé a "Todos".
        </p>
      </div>
    </div>

    <!-- Recorridos Guiados: ahora viven DENTRO del carousel de Turismo (cards .exp-card con isTour) -->
      <!-- (Sección estática de tours removida; ahora viven dentro del carousel de Turismo) -->

  <!-- Museum/Guide Details Modal (Overlay) -->
  <div
    id="guide-modal"
    class="fixed inset-0 z-50 flex hidden items-center justify-center bg-black/60 p-4 backdrop-blur-md transition-opacity duration-300 opacity-0"
  >
    <div
      class="w-full max-w-lg scale-95 transform rounded-2xl sm:rounded-[2rem] border border-white/20 bg-white/90 dark:border-slate-800/60 dark:bg-slate-900/90 p-4 sm:p-6 shadow-2xl backdrop-blur-2xl transition-all duration-300 flex flex-col max-h-[calc(100vh-32px)] sm:max-h-[90vh] overflow-hidden"
    >
      <!-- Modal Header -->
      <div class="flex items-start justify-between gap-4 pb-3 border-b border-slate-100 dark:border-slate-850">
        <div>
          <span id="guide-modal-badge" class="inline-flex items-center rounded-full bg-brand-50 dark:bg-brand-950/40 text-brand-700 dark:text-brand-400 px-2.5 py-0.5 text-[10px] font-bold uppercase tracking-wider">
            Museo
          </span>
          <h2 id="guide-modal-title" class="text-lg font-bold text-slate-800 dark:text-white mt-1">
            Cargando...
          </h2>
        </div>
        <button
          type="button"
          id="btn-close-guide"
          class="rounded-full p-1.5 hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-500 dark:text-slate-400 flex items-center justify-center transition-colors"
        >
          <span class="material-symbols-outlined text-[20px]">close</span>
        </button>
      </div>

      <!-- Modal Body (Scrollable) -->
      <div class="flex-1 overflow-y-auto py-4 space-y-4 no-scrollbar">
        <!-- Image Banner -->
        <div class="relative h-36 sm:h-44 w-full overflow-hidden rounded-xl sm:rounded-2xl bg-slate-100 dark:bg-slate-850">
          <img
            id="guide-modal-image"
            src=""
            alt=""
            class="h-full w-full object-cover"
          />
        </div>

        <!-- Language Selector Tabs -->
        <div class="flex gap-2 p-1 bg-slate-150 dark:bg-slate-800 rounded-xl">
          <button
            type="button"
            data-guide-lang="es"
            class="guide-lang-tab flex-1 py-2 text-[11px] font-bold rounded-lg transition-all text-brand-700 dark:text-brand-300 bg-white dark:bg-slate-700 shadow-sm"
          >
            🇪🇸 ES
          </button>
          <button
            type="button"
            data-guide-lang="en"
            class="guide-lang-tab flex-1 py-2 text-[11px] font-bold rounded-lg transition-all text-slate-500 dark:text-slate-400 hover:text-slate-700 dark:hover:text-slate-350"
          >
            🇺🇸 EN
          </button>
          <button
            type="button"
            data-guide-lang="señas"
            class="guide-lang-tab flex-1 py-2 text-[11px] font-bold rounded-lg transition-all text-slate-500 dark:text-slate-400 hover:text-slate-700 dark:hover:text-slate-350"
          >
            🤟 LSM
          </button>
        </div>

        <!-- Translated Content -->
        <div class="space-y-2">
          <h3 id="guide-modal-subtitle" class="font-bold text-[12px] text-slate-700 dark:text-slate-300 uppercase tracking-wider">
            Guía de Audio e Información
          </h3>
          <p id="guide-modal-text" class="text-xs leading-relaxed text-slate-600 dark:text-slate-300">
            Cargando...
          </p>
        </div>

        <!-- LSM Video Container -->
        <div id="guide-modal-video-container" class="hidden space-y-2">
          <h4 class="font-bold text-[11px] text-slate-700 dark:text-slate-300">
            Intérprete en Lengua de Señas Mexicana (LSM)
          </h4>
          <div class="relative w-full aspect-video rounded-xl overflow-hidden bg-black border border-slate-200 dark:border-slate-800">
            <iframe
              id="guide-modal-video"
              class="absolute inset-0 w-full h-full"
              src=""
              title="LSM Guide Video"
              frameborder="0"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
              allowfullscreen
            ></iframe>
          </div>
        </div>
      </div>
      
      <!-- Modal Footer -->
      <div class="pt-3 border-t border-slate-100 dark:border-slate-800/80 flex items-center justify-between">
        <div class="flex items-center gap-1">
          <span class="material-symbols-outlined text-[16px] text-amber-500" style="font-variation-settings:'FILL' 1">star</span>
          <span id="guide-modal-rating" class="text-xs font-bold text-slate-800 dark:text-slate-200">4.97</span>
        </div>
        <button
          type="button"
          id="btn-go-to-map-route"
          class="inline-flex items-center gap-1.5 rounded-xl bg-[#3f043a] hover:bg-[#3f043a]/90 text-white font-bold px-4 py-2 text-xs shadow-sm transition-all active:scale-[0.97]"
        >
          <span class="material-symbols-outlined text-[16px]">map</span>
          <span>Ver ruta en el mapa</span>
        </button>
      </div>
    </div>
  </div>

  <!-- Floating Cart Button -->
  <button
    type="button"
    id="btn-cart-toggle"
    class="fixed bottom-[76px] left-4 z-40 flex h-14 w-14 items-center justify-center rounded-full bg-purple-600 text-white shadow-lg shadow-purple-500/30 backdrop-blur-md transition-all active:scale-90 hover:bg-purple-700 focus:outline-none border border-white/20 hidden"
    aria-label="Abrir carrito de compras"
  >
    <span class="material-symbols-outlined text-[24px]">shopping_cart</span>
    <span
      id="cart-badge"
      class="absolute -top-1 -right-1 hidden h-5 w-5 items-center justify-center rounded-full bg-rose-500 text-[10px] font-bold text-white ring-2 ring-white"
    >
      0
    </span>
  </button>

  <!-- Cart Sidebar Drawer -->
  <div
    id="cart-drawer"
    class="fixed inset-0 z-50 flex justify-end bg-black/60 backdrop-blur-sm transition-opacity duration-300 opacity-0 hidden"
  >
    <div
      id="cart-drawer-content"
      class="h-full w-full max-w-md translate-x-full transform border-l border-slate-200/50 bg-white/95 dark:border-slate-800/50 dark:bg-slate-900/95 p-6 shadow-2xl backdrop-blur-2xl transition-all duration-300 flex flex-col justify-between"
    >
      <!-- Header -->
      <div class="flex items-center justify-between border-b border-slate-100 dark:border-slate-800/60 pb-4">
        <div class="flex items-center gap-2">
          <span class="material-symbols-outlined text-purple-600 dark:text-purple-400">shopping_bag</span>
          <h2 class="text-lg font-bold text-slate-800 dark:text-white">Mi Carrito</h2>
        </div>
        <button
          type="button"
          id="btn-close-cart"
          class="rounded-full p-1.5 hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-500 dark:text-slate-400 flex items-center justify-center transition-colors"
        >
          <span class="material-symbols-outlined text-[20px]">close</span>
        </button>
      </div>

      <!-- Cart Items (Scrollable) -->
      <div id="cart-items-container" class="flex-1 overflow-y-auto py-4 space-y-4 no-scrollbar">
        <div id="cart-empty-state" class="flex flex-col items-center justify-center h-full text-center space-y-2 py-8">
          <span class="material-symbols-outlined text-4xl text-slate-350">production_quantity_limits</span>
          <p class="text-sm font-semibold text-slate-700 dark:text-slate-300">Tu carrito está vacío</p>
          <p class="text-[12px] text-slate-500">¡Explora la tienda y añade hermosas artesanías mexicanas!</p>
        </div>
      </div>

      <!-- Footer / Summary -->
      <div class="border-t border-slate-100 dark:border-slate-800/60 pt-4 space-y-4">
        <div class="space-y-1.5 text-sm">
          <div class="flex justify-between text-slate-500 dark:text-slate-400">
            <span>Subtotal</span>
            <span id="cart-subtotal">$0.00 MXN</span>
          </div>
          <div class="flex justify-between text-slate-500 dark:text-slate-400">
            <span>Envío</span>
            <span class="text-emerald-600 dark:text-emerald-400 font-semibold">Gratis</span>
          </div>
          <div class="flex justify-between text-base font-bold text-slate-800 dark:text-white pt-1.5 border-t border-dashed border-slate-150 dark:border-slate-800/60">
            <span>Total</span>
            <span id="cart-total">$0.00 MXN</span>
          </div>
        </div>

        <button
          type="button"
          id="btn-checkout"
          class="w-full inline-flex items-center justify-center gap-2 rounded-2xl bg-purple-600 hover:bg-purple-700 text-white font-bold py-3.5 text-xs shadow-md transition-all active:scale-[0.98]"
        >
          <span class="material-symbols-outlined text-[18px]">chat</span>
          <span>Pedir por WhatsApp</span>
        </button>
      </div>
    </div>
  </div>

  <!-- Product Details Modal (Overlay) -->
  <div
    id="product-modal"
    class="fixed inset-0 z-50 flex hidden items-center justify-center bg-black/60 p-4 backdrop-blur-md transition-opacity duration-300 opacity-0"
  >
    <div
      class="w-full max-w-lg scale-95 transform rounded-2xl sm:rounded-[2rem] border border-white/20 bg-white/90 dark:border-slate-800/60 dark:bg-slate-900/90 p-4 sm:p-6 shadow-2xl backdrop-blur-2xl transition-all duration-300 flex flex-col max-h-[calc(100vh-32px)] sm:max-h-[90vh] overflow-hidden"
    >
      <!-- Modal Header -->
      <div class="flex items-start justify-between gap-4 pb-3 border-b border-slate-100 dark:border-slate-850">
        <div>
          <span class="inline-flex items-center rounded-full bg-purple-50 dark:bg-purple-950/40 text-purple-700 dark:text-purple-400 px-2.5 py-0.5 text-[10px] font-bold uppercase tracking-wider border border-purple-200/50">
            Tianquiztlis Mercado
          </span>
          <h2 id="product-modal-title" class="text-lg font-bold text-slate-800 dark:text-white mt-1">
            Nombre del Producto
          </h2>
        </div>
        <button
          type="button"
          id="btn-close-product"
          class="rounded-full p-1.5 hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-500 dark:text-slate-400 flex items-center justify-center transition-colors"
        >
          <span class="material-symbols-outlined text-[20px]">close</span>
        </button>
      </div>

      <!-- Modal Body (Scrollable) -->
      <div class="flex-1 overflow-y-auto py-4 space-y-4 no-scrollbar">
        <!-- Image Gallery / Slideshow -->
        <div class="relative h-44 sm:h-60 w-full overflow-hidden rounded-xl sm:rounded-2xl bg-slate-100 dark:bg-slate-850">
          <img
            id="product-modal-image"
            src=""
            alt=""
            class="h-full w-full object-cover transition-all duration-300"
          />
          <!-- Navigation Arrows -->
          <button
            type="button"
            id="btn-product-prev-img"
            class="absolute left-3 top-1/2 -translate-y-1/2 rounded-full bg-white/80 dark:bg-slate-900/80 text-neutral-700 dark:text-neutral-300 backdrop-blur-sm p-1.5 transition-all shadow-sm active:scale-90"
            title="Imagen anterior"
          >
            <span class="material-symbols-outlined text-[16px] font-bold">chevron_left</span>
          </button>
          <button
            type="button"
            id="btn-product-next-img"
            class="absolute right-3 top-1/2 -translate-y-1/2 rounded-full bg-white/80 dark:bg-slate-900/80 text-neutral-700 dark:text-neutral-300 backdrop-blur-sm p-1.5 transition-all shadow-sm active:scale-90"
            title="Imagen siguiente"
          >
            <span class="material-symbols-outlined text-[16px] font-bold">chevron_right</span>
          </button>
          <!-- Dots Indicator -->
          <div id="product-image-dots" class="absolute bottom-3 left-1/2 -translate-x-1/2 flex gap-1.5"></div>
        </div>

        <!-- Price and Availability -->
        <div class="flex items-center justify-between">
          <div class="text-lg font-extrabold text-purple-600 dark:text-purple-400" id="product-modal-price">
            $0.00 MXN
          </div>
          <span class="inline-flex items-center gap-1 rounded-full bg-emerald-50 dark:bg-emerald-950/30 px-2.5 py-1 text-[10px] font-bold text-emerald-600 dark:text-emerald-400 border border-emerald-200/50">
            <span class="h-1.5 w-1.5 rounded-full bg-emerald-500"></span>
            Disponible (En Stock)
          </span>
        </div>

        <!-- Description -->
        <div class="space-y-1">
          <h3 class="font-bold text-[11px] text-slate-400 dark:text-slate-500 uppercase tracking-wider">
            Descripción del Producto
          </h3>
          <p id="product-modal-desc" class="text-xs leading-relaxed text-slate-600 dark:text-slate-300">
            Descripción detallada...
          </p>
        </div>

        <!-- Metadata -->
        <div class="flex justify-center text-[10px] text-slate-400 dark:text-slate-500 border-t border-slate-100 dark:border-slate-800/60 pt-3">
          <span>100% Artesanal / Hecho a mano</span>
        </div>
      </div>
      
      <!-- Modal Footer / Add to Cart Controls -->
      <div class="pt-3 border-t border-slate-100 dark:border-slate-800/80 flex items-center gap-3">
        <!-- Quantity Selector -->
        <div class="flex items-center border border-slate-200 dark:border-slate-800 rounded-xl px-2 py-1 bg-slate-50 dark:bg-slate-900">
          <button
            type="button"
            id="btn-qty-minus"
            class="p-1 hover:bg-slate-200 dark:hover:bg-slate-850 rounded-lg text-slate-600 dark:text-slate-400 active:scale-95 transition-all"
          >
            <span class="material-symbols-outlined text-[16px] font-bold">remove</span>
          </button>
          <span id="product-qty-value" class="px-3 text-xs font-bold text-slate-800 dark:text-slate-100 min-w-[20px] text-center">1</span>
          <button
            type="button"
            id="btn-qty-plus"
            class="p-1 hover:bg-slate-200 dark:hover:bg-slate-850 rounded-lg text-slate-600 dark:text-slate-400 active:scale-95 transition-all"
          >
            <span class="material-symbols-outlined text-[16px] font-bold">add</span>
          </button>
        </div>

        <!-- Add to Cart Button -->
        <button
          type="button"
          id="btn-add-to-cart"
          class="flex-1 inline-flex items-center justify-center gap-1.5 rounded-xl bg-purple-600 hover:bg-purple-700 text-white font-bold py-2.5 text-xs shadow-sm transition-all active:scale-[0.97]"
        >
          <span class="material-symbols-outlined text-[16px]">add_shopping_cart</span>
          <span>Añadir al Carrito</span>
        </button>
      </div>
    </div>
  </div>

  <!-- Tour Reservation Modal (Overlay) -->
  <div
    id="tour-modal"
    class="fixed inset-0 z-50 flex hidden items-end justify-center bg-black/60 p-0 backdrop-blur-md transition-opacity duration-300 opacity-0 sm:items-center sm:p-4"
    role="dialog"
    aria-modal="true"
    aria-labelledby="tour-modal-title"
  >
    <div class="w-full max-w-md scale-100 transform rounded-t-3xl border border-white/20 bg-white/95 p-5 shadow-2xl backdrop-blur-2xl transition-all duration-300 sm:rounded-[2rem] dark:border-slate-800/60 dark:bg-slate-900/95 max-h-[90vh] overflow-y-auto">
      <!-- Header -->
      <div class="flex items-start justify-between gap-3 pb-3 border-b border-amber-100 dark:border-amber-900/40">
        <div class="flex-1 min-w-0">
          <span class="inline-flex items-center gap-1 rounded-full bg-amber-50 dark:bg-amber-950/30 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-amber-700 dark:text-amber-300 border border-amber-200/50">
            <span class="material-symbols-outlined text-[12px]">tour</span>
            Recorrido guiado
          </span>
          <h2 id="tour-modal-title" class="mt-2 text-base font-extrabold text-slate-900 dark:text-white leading-tight">
            Reservar tour
          </h2>
          <p id="tour-modal-subtitle" class="mt-0.5 text-[11px] font-medium text-amber-700 dark:text-amber-300">
            —
          </p>
        </div>
        <button type="button" id="tour-modal-close" class="rounded-full p-1.5 hover:bg-slate-100 dark:hover:bg-slate-800 text-slate-500 dark:text-slate-400 transition-colors flex-shrink-0" aria-label="Cerrar">
          <span class="material-symbols-outlined text-[20px]">close</span>
        </button>
      </div>

      <!-- Tour summary -->
      <div class="mt-3 rounded-2xl bg-amber-50/60 dark:bg-amber-950/20 border border-amber-100 dark:border-amber-900/40 p-3">
        <p class="text-[11px] text-amber-900 dark:text-amber-200 leading-relaxed">
          <strong>Incluye:</strong> guía e intérprete para los acompañantes.<br/>
          <strong>Precio:</strong> $480 MXN por persona.<br/>
          <strong>Guía:</strong> Mtro. Henryk Karol Kocyba (Universidad de Varsovia).
        </p>
      </div>

      <!-- Form -->
      <form id="tour-reservation-form" class="mt-4 space-y-3">
        <input type="hidden" id="tour-id-input" name="tourId" />

        <div>
          <label for="tour-name" class="block text-[11px] font-bold text-slate-700 dark:text-slate-300 mb-1">Nombre completo</label>
          <input type="text" id="tour-name" name="name" required class="text-[16px] w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 px-3 py-2.5 text-slate-800 dark:text-white placeholder-slate-400 outline-none focus:border-amber-400 focus:ring-1 focus:ring-amber-400" placeholder="Tu nombre" />
        </div>

        <div>
          <label for="tour-email" class="block text-[11px] font-bold text-slate-700 dark:text-slate-300 mb-1">Email</label>
          <input type="email" id="tour-email" name="email" required class="text-[16px] w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 px-3 py-2.5 text-slate-800 dark:text-white placeholder-slate-400 outline-none focus:border-amber-400 focus:ring-1 focus:ring-amber-400" placeholder="tu@email.com" />
        </div>

        <div>
          <label for="tour-phone" class="block text-[11px] font-bold text-slate-700 dark:text-slate-300 mb-1">Teléfono</label>
          <input type="tel" id="tour-phone" name="phone" required class="text-[16px] w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 px-3 py-2.5 text-slate-800 dark:text-white placeholder-slate-400 outline-none focus:border-amber-400 focus:ring-1 focus:ring-amber-400" placeholder="+52 55 1234 5678" />
        </div>

        <div class="grid grid-cols-2 gap-3">
          <div>
            <label for="tour-date" class="block text-[11px] font-bold text-slate-700 dark:text-slate-300 mb-1">Fecha preferida</label>
            <input type="date" id="tour-date" name="date" required class="text-[16px] w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 px-3 py-2.5 text-slate-800 dark:text-white outline-none focus:border-amber-400 focus:ring-1 focus:ring-amber-400" />
          </div>
          <div>
            <label for="tour-people" class="block text-[11px] font-bold text-slate-700 dark:text-slate-300 mb-1">Personas</label>
            <input type="number" id="tour-people" name="people" required min="1" max="20" value="2" class="text-[16px] w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 px-3 py-2.5 text-slate-800 dark:text-white outline-none focus:border-amber-400 focus:ring-1 focus:ring-amber-400" />
          </div>
        </div>

        <div>
          <label for="tour-notes" class="block text-[11px] font-bold text-slate-700 dark:text-slate-300 mb-1">Notas (opcional)</label>
          <textarea id="tour-notes" name="notes" rows="2" class="text-[16px] w-full rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 px-3 py-2 text-slate-800 dark:text-white placeholder-slate-400 outline-none focus:border-amber-400 focus:ring-1 focus:ring-amber-400 resize-none" placeholder="Algún requerimiento especial..."></textarea>
        </div>

        <!-- Total -->
        <div class="flex items-center justify-between rounded-xl bg-slate-100 dark:bg-slate-800 p-3">
          <span class="text-[12px] font-bold text-slate-700 dark:text-slate-300">Total estimado</span>
          <span id="tour-total" class="text-lg font-extrabold text-amber-700 dark:text-amber-400">$960 MXN</span>
        </div>

        <!-- Submit -->
        <button type="submit" id="tour-submit-btn" class="w-full inline-flex items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-amber-500 to-orange-600 hover:from-amber-600 hover:to-orange-700 text-white font-bold py-3 text-sm shadow-sm transition-all active:scale-[0.97]">
          <span class="material-symbols-outlined text-[18px]">send</span>
          Enviar solicitud de reserva
        </button>
      </form>

      <!-- Success state (hidden by default) -->
      <div id="tour-success-state" class="hidden mt-4 rounded-2xl bg-emerald-50 dark:bg-emerald-950/30 border border-emerald-200 dark:border-emerald-800/50 p-5 text-center">
        <span class="material-symbols-outlined text-4xl text-emerald-600 dark:text-emerald-400">check_circle</span>
        <h3 class="mt-2 text-base font-bold text-emerald-900 dark:text-emerald-200">¡Solicitud enviada!</h3>
        <p class="mt-1 text-[12px] text-emerald-800 dark:text-emerald-300 leading-relaxed">
          Te contactaremos por email y WhatsApp para confirmar disponibilidad y coordinar el pago.
        </p>
        <button type="button" id="tour-success-close" class="mt-4 w-full inline-flex items-center justify-center gap-1.5 rounded-xl bg-emerald-600 hover:bg-emerald-700 text-white font-bold py-2 text-[12px] transition-all">
          Entendido
        </button>
      </div>
    </div>
  </div>

  </Layout>

<style>
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  .filter-chip[aria-pressed='true'] {
    background-color: #3f043a;
    color: #ffffff;
    border-color: #3f043a;
  }

  .exp-card.hidden-card {
    display: none !important;
  }
  .category-section.hidden-section {
    display: none !important;
  }

  @keyframes bounce-slow {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-6px);
    }
  }
  .animate-bounce-slow {
    animation: bounce-slow 4s infinite ease-in-out;
  }
</style>

<script>
  // Filtrado y búsqueda client-side de experiencias con secciones horizontales.
  const chips = document.querySelectorAll<HTMLButtonElement>('.filter-chip');
  const sections = document.querySelectorAll<HTMLElement>('.category-section');
  const emptyState = document.getElementById('empty-state');
  const searchInput = document.getElementById('explore-search-input') as HTMLInputElement;

  let activeCategory = 'todos';
  let searchQuery = '';

  function updateList() {
    let totalVisibleCount = 0;

    sections.forEach((section) => {
      const sectionCategory = section.dataset.category || '';
      const cards = section.querySelectorAll<HTMLElement>('.exp-card');
      let visibleCardsInSection = 0;

      cards.forEach((card) => {
        const title = card.querySelector('h2')?.textContent?.toLowerCase() || '';
        const desc = card.querySelector('p')?.textContent?.toLowerCase() || '';

        const matchesCategory = activeCategory === 'todos' || sectionCategory === activeCategory;
        const matchesSearch = title.includes(searchQuery) || desc.includes(searchQuery);

        const isVisible = matchesCategory && matchesSearch;
        card.classList.toggle('hidden-card', !isVisible);
        if (isVisible) {
          visibleCardsInSection++;
        }
      });

      // Si la categoría de la sección no coincide con la activa, o si no hay cartas visibles en ella, ocultamos toda la sección
      const isSectionVisible = (activeCategory === 'todos' || sectionCategory === activeCategory) && (visibleCardsInSection > 0);
      section.classList.toggle('hidden-section', !isSectionVisible);

      totalVisibleCount += visibleCardsInSection;
    });

    if (emptyState) {
      emptyState.classList.toggle('hidden', totalVisibleCount > 0);
      emptyState.classList.toggle('flex', totalVisibleCount === 0);
    }
  }

  chips.forEach((chip) => {
    chip.addEventListener('click', () => {
      chips.forEach((c) => {
        c.setAttribute('aria-pressed', 'false');
        c.classList.remove('shadow-sm');
      });
      chip.setAttribute('aria-pressed', 'true');
      chip.classList.add('shadow-sm');
      activeCategory = chip.dataset.filter ?? 'todos';
      updateList();
    });
  });

  searchInput?.addEventListener('input', (e) => {
    searchQuery = (e.target as HTMLInputElement).value.toLowerCase().trim();
    updateList();
  });

  // Manejar el scroll horizontal con los botones chevron
  document.querySelectorAll('.category-section').forEach((section) => {
    const container = section.querySelector('.scroll-container');
    const leftBtn = section.querySelector('.scroll-left-btn');
    const rightBtn = section.querySelector('.scroll-right-btn');

    if (container) {
      leftBtn?.addEventListener('click', () => {
        container.scrollBy({ left: -280, behavior: 'smooth' });
      });
      rightBtn?.addEventListener('click', () => {
        container.scrollBy({ left: 280, behavior: 'smooth' });
      });
    }
  });

  // Hacer interactivos los botones de favoritos (corazón)
  document.querySelectorAll('.favorite-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const svg = btn.querySelector('svg');
      if (svg) {
        const isFavorited = svg.classList.contains('fill-rose-500');
        if (isFavorited) {
          svg.classList.remove('fill-rose-500', 'text-rose-500');
          svg.classList.add('fill-none', 'text-neutral-700', 'dark:text-neutral-300');
        } else {
          svg.classList.remove('fill-none', 'text-neutral-700', 'dark:text-neutral-300');
          svg.classList.add('fill-rose-500', 'text-rose-500');
        }
      }
    });
  });

  // Lógica del modal de guías y traducciones
  const guideModal = document.getElementById('guide-modal');
  const btnCloseGuide = document.getElementById('btn-close-guide');
  const btnGoToMapRoute = document.getElementById('btn-go-to-map-route');
  
  const guideTitle = document.getElementById('guide-modal-title');
  const guideBadge = document.getElementById('guide-modal-badge');
  const guideImage = document.getElementById('guide-modal-image') as HTMLImageElement;
  const guideText = document.getElementById('guide-modal-text');
  const guideRating = document.getElementById('guide-modal-rating');
  const guideVideoContainer = document.getElementById('guide-modal-video-container');
  const guideVideo = document.getElementById('guide-modal-video') as HTMLIFrameElement;
  
  const langTabs = document.querySelectorAll<HTMLButtonElement>('.guide-lang-tab');

  let currentTranslations: any = null;
  let activeGuideLang = 'es';
  let currentMuseumId = '';
  let currentIsVR = false;
  let currentVRUrl = '';

  function openGuideModal(card: HTMLElement) {
    const rawTranslations = card.getAttribute('data-translations');
    if (!rawTranslations) return;

    try {
      currentTranslations = JSON.parse(rawTranslations);
    } catch (e) {
      console.error('Error parsing translations JSON:', e);
      return;
    }

    currentMuseumId = card.getAttribute('data-id') || '';
    const rating = card.getAttribute('data-rating') || '4.8';
    const imageSrc = card.getAttribute('data-image') || '';

    if (guideTitle) guideTitle.textContent = currentTranslations.es.name;
    if (guideImage) {
      guideImage.src = imageSrc;
      guideImage.alt = currentTranslations.es.name;
    }
    if (guideRating) guideRating.textContent = parseFloat(rating).toFixed(2);

    // Default a español
    setGuideLanguage('es');

    // Si la card tiene experiencia VR, mostrar el iframe en el slot del video
    // y renombrar el título del bloque. La lógica de tabs sigue funcionando encima.
    currentIsVR = card.getAttribute('data-is-vr') === 'true';
    currentVRUrl = card.getAttribute('data-vr-url') || '';
    const vrTitle = guideVideoContainer?.querySelector('h4');
    if (currentIsVR && currentVRUrl && guideVideoContainer && guideVideo) {
      guideVideoContainer.classList.remove('hidden');
      if (vrTitle) vrTitle.textContent = 'Experiencia Inmersiva VR (YouTube 360°)';
      guideVideo.src = currentVRUrl;
    }

    if (guideModal) {
      guideModal.classList.remove('hidden');
      // Forzar reflow
      void guideModal.offsetHeight;
      guideModal.classList.remove('opacity-0');
      const innerContent = guideModal.querySelector('div');
      if (innerContent) {
        innerContent.classList.remove('scale-95');
        innerContent.classList.add('scale-100');
      }
    }
  }

  function closeGuideModal() {
    if (guideModal) {
      guideModal.classList.add('opacity-0');
      const innerContent = guideModal.querySelector('div');
      if (innerContent) {
        innerContent.classList.remove('scale-100');
        innerContent.classList.add('scale-95');
      }
      setTimeout(() => {
        guideModal.classList.add('hidden');
        if (guideVideo) guideVideo.src = ''; // Detener video si se está reproduciendo
        currentIsVR = false;
        currentVRUrl = '';
      }, 300);
    }
  }

  function setGuideLanguage(lang: string) {
    activeGuideLang = lang;
    if (!currentTranslations || !currentTranslations[lang]) return;

    const data = currentTranslations[lang];

    if (guideTitle) guideTitle.textContent = data.name;
    if (guideBadge) guideBadge.textContent = data.category;
    if (guideText) guideText.textContent = data.guide;

    // Actualizar tabs activas visualmente
    langTabs.forEach((tab) => {
      const isSelected = tab.getAttribute('data-guide-lang') === lang;
      tab.classList.toggle('bg-white', isSelected);
      tab.classList.toggle('dark:bg-slate-700', isSelected);
      tab.classList.toggle('shadow-sm', isSelected);
      tab.classList.toggle('text-brand-700', isSelected);
      tab.classList.toggle('dark:text-brand-300', isSelected);
      
      tab.classList.toggle('text-slate-500', !isSelected);
      tab.classList.toggle('dark:text-slate-400', !isSelected);
      tab.classList.toggle('hover:text-slate-700', !isSelected);
      tab.classList.toggle('dark:hover:text-slate-350', !isSelected);
    });

    // Manejar video de LSM o VR
    if (currentIsVR && currentVRUrl) {
      // Experiencia VR: el iframe queda fijo, los tabs solo cambian el texto guía
      if (guideVideoContainer) guideVideoContainer.classList.remove('hidden');
      if (guideVideo) guideVideo.src = currentVRUrl;
    } else if (lang === 'señas' && data.videoUrl) {
      if (guideVideoContainer) guideVideoContainer.classList.remove('hidden');
      if (guideVideo) guideVideo.src = data.videoUrl;
    } else {
      if (guideVideoContainer) guideVideoContainer.classList.add('hidden');
      if (guideVideo) guideVideo.src = '';
    }
  }

  // Escuchar clics en tarjetas (museos y tienda)
  document.querySelectorAll<HTMLElement>('.exp-card').forEach((card) => {
    card.addEventListener('click', (e) => {
      // Ignorar si pulsó el botón de favoritos u otros botones internos
      const target = e.target as HTMLElement;
      if (target.closest('.favorite-btn') || target.closest('.guide-trigger-btn') || target.closest('.product-trigger-btn')) {
        if (target.closest('.favorite-btn')) return;
      }

      const isProduct = card.getAttribute('data-is-product') === 'true';
      if (isProduct) {
        openProductModal(card);
      } else if (card.getAttribute('data-translations')) {
        openGuideModal(card);
      }
    });
  });

  // También escuchar en botones específicos de acción
  document.querySelectorAll<HTMLButtonElement>('.guide-trigger-btn').forEach((btn) => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const card = btn.closest('.exp-card') as HTMLElement;
      if (card) openGuideModal(card);
    });
  });

  document.querySelectorAll<HTMLButtonElement>('.product-trigger-btn').forEach((btn) => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const card = btn.closest('.exp-card') as HTMLElement;
      if (card) openProductModal(card);
    });
  });

  // Escuchar clics en los tabs de idioma del modal de guía
  langTabs.forEach((tab) => {
    tab.addEventListener('click', () => {
      const lang = tab.getAttribute('data-guide-lang');
      if (lang) setGuideLanguage(lang);
    });
  });

  btnCloseGuide?.addEventListener('click', closeGuideModal);
  
  // Cerrar haciendo clic afuera del modal de guía
  guideModal?.addEventListener('click', (e) => {
    if (e.target === guideModal) {
      closeGuideModal();
    }
  });

  // Redirigir al mapa con la ruta activa
  btnGoToMapRoute?.addEventListener('click', () => {
    if (currentMuseumId) {
      window.location.href = `/mapa?route=${currentMuseumId}&play=true`;
    }
  });

  // --- LÓGICA DE TIENDA Y CARRITO DE COMPRAS ---

  // Elementos de la Tienda
  const productModal = document.getElementById('product-modal');
  const btnCloseProduct = document.getElementById('btn-close-product');
  const productTitle = document.getElementById('product-modal-title');
  const productImage = document.getElementById('product-modal-image') as HTMLImageElement;
  const productPrice = document.getElementById('product-modal-price');
  const productDesc = document.getElementById('product-modal-desc');
  const productSku = document.getElementById('product-modal-sku');
  const productImageDots = document.getElementById('product-image-dots');
  const btnProductPrevImg = document.getElementById('btn-product-prev-img');
  const btnProductNextImg = document.getElementById('btn-product-next-img');
  
  const btnQtyMinus = document.getElementById('btn-qty-minus');
  const btnQtyPlus = document.getElementById('btn-qty-plus');
  const qtyValueEl = document.getElementById('product-qty-value');
  const btnAddToCart = document.getElementById('btn-add-to-cart');

  const btnCartToggle = document.getElementById('btn-cart-toggle');
  const btnCloseCart = document.getElementById('btn-close-cart');
  const cartDrawer = document.getElementById('cart-drawer');
  const cartDrawerContent = document.getElementById('cart-drawer-content');
  const cartItemsContainer = document.getElementById('cart-items-container');
  const cartEmptyState = document.getElementById('cart-empty-state');
  const cartSubtotalEl = document.getElementById('cart-subtotal');
  const cartTotalEl = document.getElementById('cart-total');
  const cartBadge = document.getElementById('cart-badge');
  const btnCheckout = document.getElementById('btn-checkout');

  // Estado de la Tienda
  interface CartItem {
    sku: string;
    name: string;
    price: number;
    image: string;
    qty: number;
  }

  let cart: CartItem[] = [];
  let productImages: string[] = [];
  let currentImageIdx = 0;
  let currentQty = 1;
  let activeProduct: { sku: string; name: string; price: number; image: string } | null = null;

  // Cargar Carrito desde LocalStorage
  function loadCart() {
    try {
      const stored = localStorage.getItem('edificarte_cart');
      if (stored) {
        cart = JSON.parse(stored);
      }
    } catch (e) {
      console.error('Error loading cart:', e);
      cart = [];
    }
    updateCartUI();
  }

  // Guardar Carrito en LocalStorage
  function saveCart() {
    localStorage.setItem('edificarte_cart', JSON.stringify(cart));
    updateCartUI();
  }

  // Actualizar la interfaz del carrito y contador
  function updateCartUI() {
    const totalItems = cart.reduce((acc, item) => acc + item.qty, 0);
    const subtotal = cart.reduce((acc, item) => acc + (item.price * item.qty), 0);

    // Actualizar badge del botón flotante e integrar visibilidad del botón en sí
    if (cartBadge) {
      cartBadge.textContent = totalItems.toString();
      cartBadge.classList.toggle('hidden', totalItems === 0);
      cartBadge.classList.toggle('flex', totalItems > 0);
    }
    if (btnCartToggle) {
      btnCartToggle.classList.toggle('hidden', totalItems === 0);
      btnCartToggle.classList.toggle('flex', totalItems > 0);
    }

    // Renderizar items del carrito
    if (cartItemsContainer) {
      // Remover todo excepto el empty state
      const itemEls = cartItemsContainer.querySelectorAll('.cart-item-row');
      itemEls.forEach(el => el.remove());

      if (cart.length === 0) {
        if (cartEmptyState) cartEmptyState.classList.remove('hidden');
      } else {
        if (cartEmptyState) cartEmptyState.classList.add('hidden');

        cart.forEach((item) => {
          const row = document.createElement('div');
          row.className = 'cart-item-row flex items-center gap-3 p-3 rounded-2xl bg-slate-50 dark:bg-slate-850 border border-slate-100 dark:border-slate-800/40';

          // Helper local para construir elementos sin innerHTML (XSS hardening)
          const el = (tag: string, className: string, text?: string): HTMLElement => {
            const node = document.createElement(tag);
            if (className) node.className = className;
            if (text !== undefined) node.textContent = text;
            return node;
          };

          const img = document.createElement('img');
          img.src = item.image;
          img.alt = item.name;
          img.className = 'h-16 w-16 rounded-xl object-cover bg-slate-100 dark:bg-slate-800 flex-none';
          row.appendChild(img);

          const info = el('div', 'flex-1 min-w-0');
          const title = el('h4', 'text-xs font-bold text-slate-800 dark:text-white truncate', item.name);
          info.appendChild(title);

          const price = el('p', 'text-[11px] font-extrabold text-purple-600 dark:text-purple-400 mt-0.5', `$${item.price.toFixed(2)} MXN`);
          info.appendChild(price);

          const controlsRow = el('div', 'flex items-center justify-between mt-2');

          const qtyBox = el('div', 'flex items-center border border-slate-200 dark:border-slate-700 rounded-lg px-1.5 py-0.5 bg-white dark:bg-slate-900');

          const btnMinus = document.createElement('button');
          btnMinus.type = 'button';
          btnMinus.className = 'btn-cart-minus p-0.5 hover:bg-slate-100 dark:hover:bg-slate-800 rounded text-slate-500 active:scale-90';
          btnMinus.setAttribute('data-sku', item.sku);
          const minusIcon = el('span', 'material-symbols-outlined text-[12px] font-bold', 'remove');
          btnMinus.appendChild(minusIcon);
          qtyBox.appendChild(btnMinus);

          const qtyLabel = el('span', 'px-2 text-[11px] font-bold text-slate-700 dark:text-slate-200', String(item.qty));
          qtyBox.appendChild(qtyLabel);

          const btnPlus = document.createElement('button');
          btnPlus.type = 'button';
          btnPlus.className = 'btn-cart-plus p-0.5 hover:bg-slate-100 dark:hover:bg-slate-800 rounded text-slate-500 active:scale-90';
          btnPlus.setAttribute('data-sku', item.sku);
          const plusIcon = el('span', 'material-symbols-outlined text-[12px] font-bold', 'add');
          btnPlus.appendChild(plusIcon);
          qtyBox.appendChild(btnPlus);

          controlsRow.appendChild(qtyBox);

          const btnRemove = document.createElement('button');
          btnRemove.type = 'button';
          btnRemove.className = 'btn-cart-remove text-rose-500 hover:text-rose-600 p-1 rounded-full hover:bg-rose-50 dark:hover:bg-rose-950/20 active:scale-90 transition-all';
          btnRemove.setAttribute('data-sku', item.sku);
          btnRemove.title = 'Eliminar artículo';
          const removeIcon = el('span', 'material-symbols-outlined text-[16px]', 'delete');
          btnRemove.appendChild(removeIcon);

          controlsRow.appendChild(btnRemove);
          info.appendChild(controlsRow);
          row.appendChild(info);

          // Bindear eventos
          btnMinus.addEventListener('click', () => {
            if (item.qty > 1) {
              updateItemQty(item.sku, item.qty - 1);
            } else {
              removeItem(item.sku);
            }
          });

          btnPlus.addEventListener('click', () => {
            updateItemQty(item.sku, item.qty + 1);
          });

          btnRemove.addEventListener('click', () => {
            removeItem(item.sku);
          });

          cartItemsContainer.appendChild(row);
        });
      }
    }

    // Totales
    if (cartSubtotalEl) cartSubtotalEl.textContent = `$${subtotal.toFixed(2)} MXN`;
    if (cartTotalEl) cartTotalEl.textContent = `$${subtotal.toFixed(2)} MXN`;
  }

  function updateItemQty(sku: string, qty: number) {
    const item = cart.find(i => i.sku === sku);
    if (item) {
      item.qty = qty;
      saveCart();
    }
  }

  function removeItem(sku: string) {
    cart = cart.filter(i => i.sku !== sku);
    saveCart();
  }

  // Abrir Modal de Producto
  function openProductModal(card: HTMLElement) {
    const title = card.getAttribute('data-title') || '';
    const desc = card.getAttribute('data-desc') || '';
    const priceText = card.getAttribute('data-price') || '';
    const sku = card.getAttribute('data-sku') || '';
    const rawImages = card.getAttribute('data-images');
    const defaultImage = card.getAttribute('data-image') || '';

    try {
      productImages = rawImages ? JSON.parse(rawImages) : [defaultImage];
    } catch (e) {
      productImages = [defaultImage];
    }

    currentImageIdx = 0;
    currentQty = 1;

    // Parsear precio numérico
    let numericPrice = 0;
    if (priceText) {
      const match = priceText.match(/[\d.]+/);
      if (match) numericPrice = parseFloat(match[0]);
    }

    activeProduct = {
      sku: sku || title,
      name: title,
      price: numericPrice,
      image: productImages[0] || defaultImage
    };

    if (productTitle) productTitle.textContent = title;
    if (productDesc) productDesc.textContent = desc;
    if (productPrice) productPrice.textContent = priceText || 'Consultar';
    if (productSku) productSku.textContent = sku || 'N/A';
    if (qtyValueEl) qtyValueEl.textContent = currentQty.toString();

    updateProductGallery();

    if (productModal) {
      productModal.classList.remove('hidden');
      void productModal.offsetHeight;
      productModal.classList.remove('opacity-0');
      const innerContent = productModal.querySelector('div');
      if (innerContent) {
        innerContent.classList.remove('scale-95');
        innerContent.classList.add('scale-100');
      }
    }
  }

  // Cerrar Modal de Producto
  function closeProductModal() {
    if (productModal) {
      productModal.classList.add('opacity-0');
      const innerContent = productModal.querySelector('div');
      if (innerContent) {
        innerContent.classList.remove('scale-100');
        innerContent.classList.add('scale-95');
      }
      setTimeout(() => {
        productModal.classList.add('hidden');
      }, 300);
    }
  }

  // Actualizar Galería de Imágenes
  function updateProductGallery() {
    if (productImages.length === 0) return;

    if (productImage) {
      productImage.src = productImages[currentImageIdx];
      productImage.alt = activeProduct?.name || 'Producto';
    }

    // Ocultar flechas si solo hay 1 imagen
    if (btnProductPrevImg && btnProductNextImg) {
      const showArrows = productImages.length > 1;
      btnProductPrevImg.classList.toggle('hidden', !showArrows);
      btnProductNextImg.classList.toggle('hidden', !showArrows);
    }

    // Renderizar puntitos indicadores
    if (productImageDots) {
      productImageDots.innerHTML = '';
      if (productImages.length > 1) {
        productImages.forEach((_, idx) => {
          const dot = document.createElement('button');
          dot.type = 'button';
          dot.className = `h-1.5 rounded-full transition-all duration-300 ${idx === currentImageIdx ? 'w-4 bg-purple-600' : 'w-1.5 bg-slate-300 dark:bg-slate-700'}`;
          dot.addEventListener('click', () => {
            currentImageIdx = idx;
            updateProductGallery();
          });
          productImageDots.appendChild(dot);
        });
      }
    }
  }

  // Eventos de Navegación de Imágenes
  btnProductPrevImg?.addEventListener('click', () => {
    if (productImages.length > 1) {
      currentImageIdx = (currentImageIdx - 1 + productImages.length) % productImages.length;
      updateProductGallery();
    }
  });

  btnProductNextImg?.addEventListener('click', () => {
    if (productImages.length > 1) {
      currentImageIdx = (currentImageIdx + 1) % productImages.length;
      updateProductGallery();
    }
  });

  // Eventos de Cantidad
  btnQtyMinus?.addEventListener('click', () => {
    if (currentQty > 1) {
      currentQty--;
      if (qtyValueEl) qtyValueEl.textContent = currentQty.toString();
    }
  });

  btnQtyPlus?.addEventListener('click', () => {
    currentQty++;
    if (qtyValueEl) qtyValueEl.textContent = currentQty.toString();
  });

  // Agregar al Carrito
  btnAddToCart?.addEventListener('click', () => {
    if (!activeProduct) return;

    const existingIdx = cart.findIndex(item => item.sku === activeProduct!.sku);
    if (existingIdx !== -1) {
      cart[existingIdx].qty += currentQty;
    } else {
      cart.push({
        sku: activeProduct.sku,
        name: activeProduct.name,
        price: activeProduct.price,
        image: activeProduct.image,
        qty: currentQty
      });
    }

    saveCart();
    closeProductModal();

    // Feedback visual breve del botón de carrito flotante (animación de rebote)
    if (btnCartToggle) {
      btnCartToggle.classList.add('scale-110', 'bg-purple-700');
      setTimeout(() => {
        btnCartToggle.classList.remove('scale-110', 'bg-purple-700');
      }, 300);
    }
  });

  // Cerrar Modal Producto al hacer clic afuera
  productModal?.addEventListener('click', (e) => {
    if (e.target === productModal) {
      closeProductModal();
    }
  });

  btnCloseProduct?.addEventListener('click', closeProductModal);

  // --- GESTIÓN DEL DRAWER DEL CARRITO ---

  function openCartDrawer() {
    if (cartDrawer && cartDrawerContent) {
      cartDrawer.classList.remove('hidden');
      void cartDrawer.offsetHeight;
      cartDrawer.classList.remove('opacity-0');
      cartDrawerContent.classList.remove('translate-x-full');
      cartDrawerContent.classList.add('translate-x-0');
    }
  }

  function closeCartDrawer() {
    if (cartDrawer && cartDrawerContent) {
      cartDrawer.classList.add('opacity-0');
      cartDrawerContent.classList.remove('translate-x-0');
      cartDrawerContent.classList.add('translate-x-full');
      setTimeout(() => {
        cartDrawer.classList.add('hidden');
      }, 300);
    }
  }

  btnCartToggle?.addEventListener('click', openCartDrawer);
  btnCloseCart?.addEventListener('click', closeCartDrawer);
  
  cartDrawer?.addEventListener('click', (e) => {
    if (e.target === cartDrawer) {
      closeCartDrawer();
    }
  });

  // Realizar Pedido por WhatsApp
  btnCheckout?.addEventListener('click', () => {
    if (cart.length === 0) return;

    // Número de teléfono de Fundación EdificARTE (simulado)
    const phoneNumber = '5215512345678';
    
    // Crear el mensaje detallado del pedido
    let message = '¡Hola! Me interesa comprar los siguientes productos artesanales de Tianquiztlis Mercado:\n\n';
    
    cart.forEach((item, index) => {
      message += `${index + 1}. *${item.name}* (Cant: ${item.qty}) - $${(item.price * item.qty).toFixed(2)} MXN\n\n`;
    });

    const total = cart.reduce((acc, item) => acc + (item.price * item.qty), 0);
    message += `*Total del Pedido:* $${total.toFixed(2)} MXN\n`;
    message += `*Envío:* Gratis\n\n`;
    message += '¿Me podrían indicar los pasos para completar mi compra y acordar la entrega? ¡Muchas gracias!';

    const encodedText = encodeURIComponent(message);
    const waUrl = `https://wa.me/${phoneNumber}?text=${encodedText}`;

    // Abrir enlace en pestaña nueva
    window.open(waUrl, '_blank');
  });

  // Inicializar carrito en la carga
  loadCart();

  // --- TOUR RESERVATION MODAL ---

  const tourModal = document.getElementById('tour-modal');
  const tourModalClose = document.getElementById('tour-modal-close');
  const tourModalTitle = document.getElementById('tour-modal-title');
  const tourModalSubtitle = document.getElementById('tour-modal-subtitle');
  const tourIdInput = document.getElementById('tour-id-input') as HTMLInputElement | null;
  const tourForm = document.getElementById('tour-reservation-form') as HTMLFormElement | null;
  const tourTotalEl = document.getElementById('tour-total');
  const tourSuccess = document.getElementById('tour-success-state');
  const tourSuccessClose = document.getElementById('tour-success-close');
  const tourPeopleInput = document.getElementById('tour-people') as HTMLInputElement | null;
  const tourSubmitBtn = document.getElementById('tour-submit-btn') as HTMLButtonElement | null;

  const TOUR_PRICE = 480;

  function openTourModal(tourId: string, title: string, subtitle: string) {
    if (!tourModal) return;
    if (tourIdInput) tourIdInput.value = tourId;
    if (tourModalTitle) tourModalTitle.textContent = title;
    if (tourModalSubtitle) tourModalSubtitle.textContent = subtitle;
    updateTourTotal();
    if (tourSuccess) tourSuccess.classList.add('hidden');
    if (tourForm) tourForm.classList.remove('hidden');
    tourModal.classList.remove('hidden');
    void tourModal.offsetHeight;
    tourModal.classList.remove('opacity-0');
    if (tourPeopleInput) tourPeopleInput.value = '2';
    const tomorrow = new Date();
    tomorrow.setDate(tomorrow.getDate() + 1);
    const dateInput = document.getElementById('tour-date') as HTMLInputElement | null;
    if (dateInput) dateInput.min = tomorrow.toISOString().split('T')[0];
  }

  function closeTourModal() {
    if (!tourModal) return;
    tourModal.classList.add('opacity-0');
    setTimeout(() => tourModal.classList.add('hidden'), 250);
  }

  function updateTourTotal() {
    const n = parseInt(tourPeopleInput?.value || '1', 10) || 1;
    if (tourTotalEl) {
      tourTotalEl.textContent = `$${(n * TOUR_PRICE).toLocaleString('es-MX')} MXN`;
    }
  }

  document.querySelectorAll<HTMLButtonElement>('.tour-reserve-btn').forEach((btn) => {
    btn.addEventListener('click', (e) => {
      e.preventDefault();
      e.stopPropagation();
      const card = btn.closest<HTMLElement>('[data-tour-id]');
      if (!card) return;
      openTourModal(
        card.dataset.tourId || '',
        card.dataset.tourTitle || 'Tour',
        card.dataset.tourMeeting || ''
      );
    });
  });

  tourPeopleInput?.addEventListener('input', updateTourTotal);

  tourModalClose?.addEventListener('click', closeTourModal);
  tourSuccessClose?.addEventListener('click', closeTourModal);
  tourModal?.addEventListener('click', (e) => {
    if (e.target === tourModal) closeTourModal();
  });

  tourForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    if (!tourSubmitBtn || !tourForm) return;

    // Snapshot de datos de la card ANTES del await (puede re-renderizarse)
    const tourId = tourIdInput?.value || '';
    const tourCard = document.querySelector<HTMLElement>(`[data-tour-id="${tourId}"]`);
    const tourTitle = tourCard?.dataset.tourTitle || 'Tour';
    const tourImage = tourCard?.dataset.tourImage || '';

    const formData = new FormData(tourForm);
    const data = {
      tourId,
      name: formData.get('name'),
      email: formData.get('email'),
      phone: formData.get('phone'),
      date: formData.get('date'),
      people: parseInt(formData.get('people') as string, 10) || 1,
      notes: formData.get('notes') || '',
    };

    if (!data.name || !data.email || !data.phone || !data.date || !data.people) {
      alert('Por favor completá todos los campos obligatorios.');
      return;
    }

    tourSubmitBtn.disabled = true;
    const originalText = tourSubmitBtn.innerHTML;
    tourSubmitBtn.innerHTML = '<span class="material-symbols-outlined text-[18px] animate-spin">progress_activity</span> Enviando...';

    try {
      const response = await fetch('/api/reservar-tour', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      if (!response.ok) throw new Error('Error en el servidor');

      const result = await response.json();

      // Persistir reserva en localStorage para que aparezca en /yo
      try {
        const STORAGE_KEY = 'edificarte_reservations';
        const existing = JSON.parse(localStorage.getItem(STORAGE_KEY) || '[]') as Array<Record<string, unknown>>;
        const reservation = {
          id: result.reservationId || `RES-${Date.now()}`,
          tourId: data.tourId,
          tourTitle,
          tourImage,
          name: data.name,
          email: data.email,
          phone: data.phone,
          date: data.date,
          people: data.people,
          totalMXN: result.totalMXN || 0,
          status: 'pending',
          createdAt: new Date().toISOString(),
        };
        existing.push(reservation);
        localStorage.setItem(STORAGE_KEY, JSON.stringify(existing));
      } catch (storageErr) {
        console.warn('No se pudo guardar la reserva en localStorage:', storageErr);
      }

      if (tourSuccess) tourSuccess.classList.remove('hidden');
      if (tourForm) tourForm.classList.add('hidden');
      tourForm.reset();
      if (tourPeopleInput) tourPeopleInput.value = '2';
    } catch (err) {
      console.error('Error al reservar:', err);
      alert('Hubo un error al enviar la solicitud. Por favor intentá de nuevo o contactanos por WhatsApp.');
    } finally {
      tourSubmitBtn.disabled = false;
      tourSubmitBtn.innerHTML = originalText;
    }
  });
</script>
